// import { Router } from "express";
// import { asyncHandler } from "../utils/asynchandler";
// import isLoggedIn from "../middlewares/isLoggedIn.middleware";
// import { getDetailedQueueStats, getQueueHealth } from "../controllers/queue.controller";

// const queueRouter = Router();

// queueRouter.get('/health', asyncHandler(getQueueHealth));
// queueRouter.get('/stats', isLoggedIn, asyncHandler(getDetailedQueueStats));

// export default queueRouter;